<IfModule mod_rewrite.c>
    RewriteEngine On

    # Send all requests to index.php if the file/dir does not exist
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [QSA,L]
</IfModule>
